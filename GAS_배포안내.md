# GAS 배포 안내 — 서명 토큰 전환

> 접속 오류가 점점 잦아지던 **구조적 원인**을 없애는 작업입니다.
> 저장소의 `.gs` 파일은 **사본**이라, Apps Script 편집기에 붙여넣고 재배포해야 실제로 반영됩니다.

---

## 왜 하는가

### 지금까지의 구조

로그인하면 랜덤 문자열(불투명 토큰)을 만들어 **보안 시트의 Tokens 탭**에 적어 두고,
데이터 GAS 7개가 요청을 받을 때마다 인증 서버에 "이 토큰 유효해?"를 물어봤습니다.

```
브라우저 ──요청──> handover GAS ──UrlFetchApp──> 인증 GAS ──> Tokens 시트 조회
                    (7개 프로젝트가 전부 이렇게)
```

여기서 세 가지가 구조적으로 따라왔습니다.

1. **인증 서버 하나가 7개 프로젝트의 단일 병목** — 모든 요청이 여기로 몰립니다
2. **Tokens 탭을 비우면 전 세션이 죽습니다.** 그런데 죽은 토큰(`lv=0`)은 캐시되지 않아서
   (옛 조건이 `if(lv > 0)` 였습니다) **죽은 세션 하나하나가 매 요청마다 왕복을 반복**했습니다.
   재로그인하기 전까지 스스로 낫지 않는 영구 부하입니다 → **"점점 잦아진다"의 정체**
3. **인증 서버에 못 닿으면 fail-open 레벨 3** — 서버를 죽이면 누구나 관리자가 되는 구멍

### 바뀌는 구조

로그인하면 **HMAC으로 서명된 토큰**을 발급합니다. 토큰이 스스로 유효성을 증명하므로
각 GAS는 **서명만 다시 계산해 비교**하면 됩니다.

```
브라우저 ──요청──> handover GAS ──(로컬 서명 검증)──> 끝
                                  네트워크 호출 0회
```

- 인증 서버는 **로그인할 때만** 쓰입니다. 검증 트래픽이 0이 됩니다
- 인증 서버가 죽어도 **이미 로그인한 사람은 계속 씁니다**
- **Tokens 탭을 비워도 아무 일도 일어나지 않습니다** ← 이번 장애가 재발하지 않습니다

---

## 배포 순서 (이 순서대로 하셔야 무중단입니다)

### 0단계 — 공유 비밀값 만들기

인증 프로젝트(보안 시트에 붙은 스크립트) 편집기에서:

1. `baz_token_lib.gs` 내용을 **새 스크립트 파일**로 추가
2. 함수 목록에서 `bazMakeSecret_` 실행 → **실행 로그**에 나온 값을 복사
   (예: `Xk9_aBc…` 43자)

이 값을 **아래 8개 프로젝트 전부**에 동일하게 넣습니다.
각 프로젝트 → 좌측 ⚙ **프로젝트 설정** → **스크립트 속성** → 속성 추가:

| 속성 | 값 |
|---|---|
| `BAZ_TOKEN_SECRET` | 위에서 만든 43자 값 (**8개 모두 동일**) |
| `BAZ_TOKEN_EPOCH` | `1` |

> ⚠️ 비밀값이 다르면 그 프로젝트만 로그인이 안 됩니다. 붙여넣기 후 앞뒤 공백이 없는지 확인하세요.

### 1단계 — 데이터 GAS 7개 먼저 (인증보다 먼저!)

대상: `handover` · `hospital` · `hospital_issue` · `visit_log` · `inspection` · `ncare_dashboard` · `chatbot`

각 프로젝트에서:

1. `baz_token_lib.gs` 내용을 **새 스크립트 파일**로 추가
2. 저장소의 해당 `*_gas.gs` 에서 **`verifyLevel_` 함수 전체**를 복사해 기존 것과 교체
3. 같은 파일 위쪽의 `AUTH_FAILOPEN_LEVEL = 3` → **`= 1`** 로 변경
4. `bazTokenSelfTest_` 실행 → 로그에 `✅ 정상` 이 나오는지 확인
5. **배포 → 배포 관리 → ✏️ → 새 버전 → 배포**

> 이 단계에서는 **아직 아무것도 바뀌지 않습니다.** 새 코드는 서명 토큰과 예전 토큰을
> 둘 다 처리하므로, 인증 서버가 아직 예전 토큰을 발급해도 정상 동작합니다.

### 2단계 — 인증 GAS 마지막

보안 시트에 붙은 스크립트 프로젝트에서:

1. `auth_gas.gs` 내용을 붙여넣기 (기존 `doGet`/`doPost`를 대체)
2. `authSelfCheck` 실행 → 로그 확인
   ```
   Credentials 행 수: 3
   시트: Credentials✓ Tokens✓ LoginLog✓
   토큰 서명: OK
   로그인 시뮬레이션: ✅ ok (level 3)
   발급 토큰 재검증: ✅
   ```
3. **배포 → 배포 관리 → ✏️ → 새 버전 → 배포**
   (URL이 바뀌면 안 되므로 **반드시 기존 배포를 수정**하세요. 새로 만들지 마세요)

### 3단계 — 확인

1. 브라우저에서 로그아웃 후 다시 로그인
2. 보안 시트 `Tokens` 탭에 새 행이 생기고, `token` 열이 **점(`.`)이 들어간 긴 문자열**이면 성공
3. 병원 조회·현장 일정이 평소보다 빠르게 뜨는지 확인

---

## 앞으로 "전원 강제 로그아웃"이 필요할 때

**Tokens 탭을 비우지 마세요.** 이제 아무 효과가 없습니다(그리고 그게 이번 장애의 원인이었습니다).

대신:

1. 인증 프로젝트에서 `bazBumpTokenEpoch_` 실행 → 로그에 나온 숫자 확인 (예: `2`)
2. **8개 프로젝트 전부**의 스크립트 속성 `BAZ_TOKEN_EPOCH` 를 그 숫자로 변경

그 숫자보다 먼저 발급된 토큰이 **즉시 전부 무효**가 됩니다. 요청당 추가 조회가 없어
부하도 생기지 않습니다.

---

## 되돌리려면

각 프로젝트의 스크립트 속성에서 **`BAZ_TOKEN_SECRET` 을 삭제**하면 됩니다.
비밀값이 없으면 `bazVerifyLocal_` 이 `null` 을 돌려주고 **예전의 인증 서버 왕복 방식으로
자동 폴백**합니다. 코드를 되돌릴 필요가 없습니다.

---

## 검증 현황 (배포 전 자동 테스트)

실제 `.gs` 코드를 그대로 Node에 올리고 Apps Script API를 동등하게 흉내 내 계측했습니다.

| 스위트 | 결과 | 핵심 항목 |
|---|---|---|
| 서명 토큰 | **26/26** | 위조·서명변조·만료·다른 비밀값 전부 거부, 한글 이름 11종 왕복, epoch 무효화 |
| 인증 서버 | **27/27** | **Tokens 시트를 비워도 세션 유효**, 레거시 토큰 호환, 헤더 자동 인식 |
| 데이터 GAS 7종 | **84/84** | **서명 토큰 20회 요청 → 인증 왕복 0회**, 무효 토큰 20회 → 왕복 1회(예전 20회), 인증 불통 시 fail-open **레벨 1** |

부수 발견: `inspection_gas.gs` 는 `AUTH_VERIFY_URL` 을 `CFG` 객체 속성으로 두고 있어,
일괄 패치를 그대로 뒀다면 배포 즉시 `ReferenceError` 로 점검접수가 중단됐을 것입니다.
위 테스트가 잡아내 교정했습니다.
